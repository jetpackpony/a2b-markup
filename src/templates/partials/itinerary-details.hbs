<div class="itinerary-summary">
  <div class="close-button">&times;</div>
  {{> itinerary-short itinerary=itinerary}}
</div>
<div class="route-list">
  {{#each routes as |route index|}}
    <div class="route-item">
      {{> route-short route=route}}
    </div>
  {{/each}}
</div>
